services:
  - type: worker
    name: ortopedia-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: LLM_PROVIDER
        value: OPENAI
      - key: API_KEY
        sync: false
